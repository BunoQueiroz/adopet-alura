version: '3.9'
services:
  postgres:
    image: postgres
    container_name: 'data_base'
    environment:
      - POSTGRES_PASSWORD=
      - POSTGRES_USER=
      - POSTGRES_DB=
    ports:
      - "5432:5432"
